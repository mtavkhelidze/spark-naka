SparkNaka (スパークの中)
---
Inside Apache Spark: native computation engine. PoC.

---

Prepare:

```sh
cd panama && aclocal && autoconf && automake --add-missing && ./configure
```

Run:

```sh
sbt run
```
